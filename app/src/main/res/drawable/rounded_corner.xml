<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>
    <corners android:radius="20dp"/> <!-- Sesuaikan radius dengan keinginan Anda -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white"/>
</shape>
